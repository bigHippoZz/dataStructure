export class MSD {
	private static readonly R = 256;

	private static aux: string[] = [];

	static sort(list: string[]) {}

	private static _sort(list: string[], low: number, high: number, w: number) {
		const count = Array(this.R + 2);
		for (let i = low; i < high; i++) {
			count
		}
	}
}
